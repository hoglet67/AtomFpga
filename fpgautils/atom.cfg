MEMORY
{
   ROM1:  start = $A000, size = $0ffe, fill = yes, fillval = $ff;
   ROM2:  start = $AFFE, size = $0002, fill = yes, fillval = $ff;
}

SEGMENTS
{
   CODE:   load = ROM1, type = ro;
   DATA:   load = ROM2, type = ro;
}
